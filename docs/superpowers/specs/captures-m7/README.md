# M7 Garmin payload shapes (sanitized)

Field names, nesting, and types captured live from `python-garminconnect`
against a real account, then sanitized for publication: `userId` zeroed and
every numeric value rounded to one significant figure. Use these for the M7
contract design — the *shapes* are authoritative, the *values* are synthetic.
