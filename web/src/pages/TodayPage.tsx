// Placeholder — replaced by its M5 plan task.
export function TodayPage() {
  return (
    <div style={{ padding: 22 }}>
      <div className="mono-label">// COMING UP</div>
      <div style={{ marginTop: 8, color: 'var(--muted)' }}>TodayPage is built in a later task.</div>
    </div>
  );
}
